# Icônes

Icônes de l'interface (navigation, états des pièces, etc.).

Convention : SVG de préférence (redimensionnable sans perte, teintable en CSS
via `currentColor`), nommage kebab-case par contexte (ex. `nav-accueil.svg`,
`etat-lumiere-on.svg`).
