# Force l'UTF-8 dans l'en-tête Content-Type pour tous les fichiers servis par ce conteneur.
# Sans ça, le navigateur doit deviner l'encodage — cause des accents mal affichés
# (ex. "LumiÃ¨re" au lieu de "Lumière") si le HTML ne déclare pas non plus <meta charset>.
charset utf-8;
